using System;
using Xunit;

namespace LicenseKeyGenerator
{
    public class ClientValidatorTests
    {
        [Fact]
        public void CanGenerateKey()
        {
          /*  var clientValidator = new ClientValidator();
            var key = clientValidator.GenerateKey();
            Console.WriteLine(key);
            Assert.NotEmpty(key);
*/
        }
    }
}